# Local dev default matches `uvicorn ... --port 8000` run locally.
# For Vercel/Netlify, set VITE_API_URL to your deployed Render backend URL,
# e.g. https://lms-backend.onrender.com (no trailing slash).
VITE_API_URL=http://localhost:8000
